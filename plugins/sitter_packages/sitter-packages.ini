# Parameters definitions for fluid-settings
#

[sitter::sitter-packages-path]
help=the path to the list of packages, used to verify that packages are installed and not in conflict.
default=/usr/share/sitter/packages
allowed=command-line,environment-variable,configuration-file,dynamic-configuration
group=options
required

